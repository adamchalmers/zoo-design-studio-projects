sketch001 = startSketchOn(XY)
profile001 = startProfile(sketch001, at = [0, 0])
  |> yLine(length = 2.85)
  |> xLine(length = -1.79)
  |> angledLine(length = -1.87, angle = 20)
  |> xLine(length = -5.44)
  |> yLine(endAbsolute = 0)
  |> line(endAbsolute = [profileStartX(%), profileStartY(%)])
  |> close()
revolve001 = revolve(profile001, angle = 360, axis = X)
// revolve001 = revolve(profile001, angle = 360, axis = X)
